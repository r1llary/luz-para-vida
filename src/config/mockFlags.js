/**
 * Lista “Minhas células”: exibe `CELULAS_MOCK` em vez do contexto (Appwrite).
 * Coloque `true` para testar o layout com dados de exemplo; `false` em produção.
 */
export const USE_CELULAS_LIST_MOCK = true;
